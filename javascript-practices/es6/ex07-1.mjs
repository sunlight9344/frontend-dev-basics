/**
 * unamed export
 * default keywork 사용해야함
 */

export default function(a, b) {
    return a + b;
};

// 주의해야함 왜냐하면 이미 default를 써버렸거든 이름 없이
// 이름없이 오는놈이 두마리라서 에러가 남
// export default function(a, b) {
//     return a - b;
// };